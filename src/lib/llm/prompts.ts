export const SHARED_JSON_INSTRUCTIONS = [
  "Return valid JSON only.",
  "Do not include markdown fences.",
  "Do not add keys not requested.",
  "Ground claims in provided material only.",
  "If evidence is missing, explicitly mark uncertainty in the field values.",
].join(" ");
